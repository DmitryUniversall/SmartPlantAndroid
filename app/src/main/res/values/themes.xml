<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.SmartPlantAndroid" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryDark">@color/primary</item> <!-- Status bar color -->
        <item name="colorAccent">@color/text_primary_inverted</item>
        <item name="android:textColorPrimary">@color/text_primary</item>
        <item name="android:statusBarColor">@color/primary</item> <!-- Status bar color -->
    </style>

    <style name="Theme.SmartPlantAndroid" parent="Base.Theme.SmartPlantAndroid"/>
</resources>
